{% extends 'base.html.twig' %}

{% block title %}AccountUser{% endblock %}

{% block body %}
    <h1>AccountUser</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ account_user.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ account_user.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ account_user.prenom }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ account_user.email }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('account_user_index') }}">back to list</a>

    <a href="{{ path('account_user_edit', {'id': account_user.id}) }}">edit</a>

    {{ include('account_user/_delete_form.html.twig') }}
{% endblock %}
